import styled from 'styled-components';

const TableNavStyles = styled.nav`
  .button-inline {
    background-color: #fff;
    color: #333;
    &.button-active {
      background-color: #333;
      color: #fff;
    }
  }
`;

export default TableNavStyles;
